User Function MT100TOK()
	Local lRet := .t.
	/*if !l103Class
		if cA100For == '001900' .and. cLoja == '01' .and. cTipo == 'N' //.and. cEspecie == 'NFE  ' .and. cSerie == 'F03'
			lRet := .f.
			MessageBox("Para esse fornecedor só é permitido a entrada através de pré-Nota!","ATENÇÃO", 16)
		endif
	endif*/

Return(lRet)
